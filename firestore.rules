rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // This project writes to Firestore from backend only (firebase-admin).
      // Client SDK direct access is disabled by default.
      allow read, write: if false;
    }
  }
}

